import { HttpClient } from '@wix/sdk-types';
import { GetGiftCardOptions, GetGiftCardResponse, RedeemGiftCardOptions, RedeemGiftCardResponse, VoidTransactionOptions } from './ecom-v1-gift-card-gift-vouchers.universal.js';
export declare const __metadata: {
    PACKAGE_NAME: string;
};
export declare function getGiftCard(httpClient: HttpClient): GetGiftCardSignature;
interface GetGiftCardSignature {
    /**
     * Retrieves a gift card.
     * @param - Gift card code.
     * @param - Field options.
     */
    (code: string, options?: GetGiftCardOptions | undefined): Promise<GetGiftCardResponse>;
}
export declare function redeemGiftCard(httpClient: HttpClient): RedeemGiftCardSignature;
interface RedeemGiftCardSignature {
    /**
     * Redeems a gift card.
     * Creates a transaction and lowers the card balance by the transaction amount.
     * @param - Gift card code.
     * @param - Field options. `amount` and `appId` **must** be passed.
     */
    (code: string, options?: RedeemGiftCardOptions | undefined): Promise<RedeemGiftCardResponse>;
}
export declare function voidTransaction(httpClient: HttpClient): VoidTransactionSignature;
interface VoidTransactionSignature {
    /**
     * Voids a gift card transaction.
     * Transaction IDs are returned in Redeem Gift Card.
     * @param - Gift card transaction ID, as returned from Redeem Gift Card.
     * @param - Field options. `appId` **must** be passed.
     */
    (transactionId: string, options?: VoidTransactionOptions | undefined): Promise<void>;
}
export { Context, GetGiftCardOptions, GetGiftCardRequest, GetGiftCardResponse, GiftCard, Money, RedeemGiftCardOptions, RedeemGiftCardRequest, RedeemGiftCardResponse, Status, VoidTransactionOptions, VoidTransactionRequest, VoidTransactionResponse, } from './ecom-v1-gift-card-gift-vouchers.universal.js';
